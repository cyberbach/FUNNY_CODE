<Window x:Class="YTVtoText.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YTVtoText"
        mc:Ignorable="d"
        Title="YouTube to Text" Height="550" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- URL Input -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="UrlTextBox" 
                     Grid.Column="0"
                     Height="30" 
                     FontSize="14"
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,20"
                     TextWrapping="NoWrap"/>
            <Button x:Name="DownloadButton" 
                    Grid.Column="1"
                    Content="Скачать" 
                    Width="100" 
                    Height="30" 
                    FontSize="14"
                    Margin="0,0,0,20"
                    Click="DownloadButton_Click"/>
        </Grid>

        <!-- Options -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <CheckBox x:Name="SaveVideoCheckBox" Content="Сохранить видео" Margin="0,0,20,0"/>
            <CheckBox x:Name="DownloadSubtitlesCheckBox" Content="Скачать субтитры" Margin="0,0,20,0" IsChecked="True"/>
            <CheckBox x:Name="RecognizeTextCheckBox" Content="Распознать текст" Margin="0,0,0,0" IsChecked="True"/>
        </StackPanel>

        <!-- Log Output -->
        <TextBox x:Name="LogTextBox" 
                 Grid.Row="2"
                 Height="350"
                 FontSize="13"
                 FontFamily="Consolas"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"/>
    </Grid>
</Window>
